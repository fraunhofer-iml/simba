# cpps-schedular-connector

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test cpps-schedular-connector` to execute the unit tests via [Jest](https://jestjs.io).
